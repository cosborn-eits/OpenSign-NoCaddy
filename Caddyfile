:3001 {
    encode gzip

    # API (Parse/OpenSign server)
    handle_path /api/* {
        reverse_proxy server:8080
    }

    # Everything else -> React client
    handle {
        reverse_proxy client:3000
    }
}
