:3001 {
    encode zstd gzip

    # All API / Parse traffic
    handle_path /app/* {
        reverse_proxy server:8080
    }

    # Everything else → front-end
    handle {
        reverse_proxy client:3000
    }
}
